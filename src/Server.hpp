#pragma once

#include <fstream>
#include <filesystem>
#include <string>

class Server
{
private:
    std::string PseudoList_path = "../ressource/PseudoList.txt";
    
public:
    Server(/* args */);
    ~Server();

};

Server::Server(/* args */)
{
}

Server::~Server()
{
}

